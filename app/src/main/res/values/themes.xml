<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.ToDoListApp" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Light theme -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variation</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Dark theme -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_variation</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Background theme -->
        <!-- <item name="android:windowBackground">@color/background</item> -->
    </style>

    <style name="Theme.ToDoListApp" parent="Base.Theme.ToDoListApp" />
</resources>